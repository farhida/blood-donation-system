"""DEPRECATED: accounts.views moved to backend/api/accounts.py

This file is intentionally replaced to prevent accidental imports. Remove it
from the repository after verifying `backend/api/accounts.py` is active and
`backend/core/views.py` imports from `api.accounts`.
"""

raise SystemExit("accounts.views moved to backend/api/accounts.py - delete this file when ready")